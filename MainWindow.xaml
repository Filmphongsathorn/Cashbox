<Window x:Class="CashboxAnalyzer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:CashboxAnalyzer.Analytics.Views"
        mc:Ignorable="d"
        Title="Cashbox Analyzer V2" Height="768" Width="1280"
        WindowStartupLocation="CenterScreen"
        Background="#E0F2FE"
        Icon="icon.png"
        FontFamily="Segoe UI">
    
    <Viewbox Stretch="Uniform">
        <Grid Width="1440" Height="900" Background="#E0F2FE">
            <TabControl Background="Transparent" BorderThickness="0" Margin="10">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border x:Name="Border" Padding="25,12" Margin="0,0,10,0" CornerRadius="12" Background="Transparent" Cursor="Hand">
                                        <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" Margin="10,2"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#3B82F6"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="False">
                                            <Setter TargetName="Border" Property="Background" Value="White"/>
                                            <Setter Property="Foreground" Value="#0F172A"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Opacity" Value="0.8"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="FontSize" Value="18"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                    </Style>
                </TabControl.Resources>
                <TabItem Header="📊 แดชบอร์ด">
                    <views:AnalyticsView x:Name="AnalyticsViewControl" />
                </TabItem>
                <TabItem Header="📝 บันทึกข้อมูล">
                    <views:DataEntryView x:Name="DataEntryViewControl" />
                </TabItem>
            </TabControl>
        </Grid>
    </Viewbox>
</Window>
